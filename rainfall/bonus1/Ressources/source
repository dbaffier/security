int main(int ac, char **av)
{
	char	buff[40];

	unsigned int len = atoi(av[1])
	if (len > 9)
		return (1);
	memcpy(buff, av[2], len * 4);
	if (len == 0x574f4c46)
	{
		execl("/bin/sh", "sh", 0);
	}
	return (0);
}
