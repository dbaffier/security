int main(void)
{
	char	*s1;
	int		v;
	int		c;
	int		u;

	if (atoi(av[1]) == 423)
	{
		s1 = strdup("/bin/sh");
		v = 0;
		c = getegid(); // esp + 0x1c
		u = getuid(); // esp + 0x18
		v = setresgid(c, u, c);
		execv("/bin/sh", s1)
		return (0);
	}
	fwrite("No !\n", 1, 5, stderr)
	return (0);
}
