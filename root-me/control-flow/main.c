#include <stdio.h>


int main(void)
{

	// (8 * key[i]) * 8
	printf("%d\n", (char)0xff ^ );
	/*for (int i = 0; i < 127; i++)
	{
		int		j = 0;
		char	c = i;
		int		count = 0;

		while (j < 8)
		{
			if (j != 0)
			{
				int div1 = c / j;
				if (div1 & 1)
					count++;
			}
			j++;
		}
		if (count == 0)
			printf("Found at %d\n", i);
		j = 0;
	}
	int local_9  = 0;

	// i = 0-8  ... 8 - 16...
	int start  = local_9 + new_key[i] + (0...7)

	start &= 1 = resultat;

	int eax = i;
	int rcx = i >> 0x1f;
	rcx = rcx >> 0x1d;
	int new = eax + rcx; //0
	new &= 7;
	new = new - rcx
	

	NEW = 0...7
	*/
}
